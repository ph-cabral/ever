name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: |
          cd ~/projects/ever
          git pull
          docker compose -f docker-compose.prod.yml up -d --build
