name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: |
          cd ~/projects/ever
          git pull
          docker compose -f docker-compose.prod.yml up -d --build